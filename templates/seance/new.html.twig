{% extends 'baseBack.html.twig'%}

{% block body %}
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <br>
                <h4 class="card-title">Ajouter Seance :</h4>
                {{ include('seance/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
